.container {
  display: flex;
  flex-direction: row;
  position: fixed;
  width: 200vw;
  left: 0;
  bottom: 0;
  transition: transform 0.5s;
  z-index: 2;
}

.slide {
  width: 100vw;
  height: auto;
}
